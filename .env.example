# Databricks SQL connectivity
DATABRICKS_HOST=your-databricks-host.azuredatabricks.net
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/your-warehouse-id
DATABRICKS_TOKEN=your-databricks-token-here

# Demo API key
API_KEY=your-api-key-here

# GraphQL mode: static | dynamic | both
GRAPHQL_MODE=dynamic

# Catalog/Schema (no hard-coded names)
CATALOG=your-catalog-name
SCHEMA=your-schema-name

# Optional: metadata registry path
DP_REGISTRY=registry
